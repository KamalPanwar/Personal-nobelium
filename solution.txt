 Q. What is the difference between git stash apply and git stash pop?
 Ans.git stash apply command leaves the top most stash on the stsh list so that we can late use it 
     git stash pop command removes or throw away the latest or the topmost stsh[D[Da